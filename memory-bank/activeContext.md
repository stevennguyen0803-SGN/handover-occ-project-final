# Active Context - OCC Handover System

> Update this file every session. Keep it short, current, and actionable.

## Current Status
- Last updated: 2026-05-05
- Status: PHASE_4_IN_PROGRESS
- Current focus: Task 4.7 Go/No-Go evidence is drafted in [docs/pilot-assessment.md](/d:/Handover%20Project/docs/pilot-assessment.md). Task 4.1 no longer requires Docker for Phase 4 acceptance; the accepted staging evidence is the no-Docker local production-mode path (`npm run build`, `npm run verify:staging:local`, browser reachability, smoke, UAT verifier, and performance evidence). In parallel, the **Friendly Redesign UI prototype** under `prototype/` and matching **React + Tailwind component stubs** under `frontend-stubs/` are available as a non-blocking design proposal. The `frontend-stubs/` package now covers Dashboard, Log, Handover Detail, New Handover wizard, **Admin Users console (BR-12)**, **Reports & Export** with print-ready CSS, and **NextAuth.js v5 (Auth.js) wiring** (Edge-safe `auth.config.ts` + Node-runtime `auth.ts` + Prisma adapter + role-aware `middleware.ts` + `<SignInForm>` / `<SignInLayout>` / `<UserMenu>` / `<RoleSwitcher>` / `<UnauthorizedView>`) — type-checked clean (`tsc --noEmit` strict on Next.js 14 + React 18 + next-auth 5.0.0-beta.20 + @auth/prisma-adapter + bcryptjs + zod) and intended to be copy-pasted into the Phase 2 Next.js frontend; it does not gate phase progression.
- Next step: Run or record the 5 human browser UAT scenarios from [docs/uat/UAT_SCENARIOS.md](/d:/Handover%20Project/docs/uat/UAT_SCENARIOS.md), add any findings to [docs/uat/DEFECT_LOG.md](/d:/Handover%20Project/docs/uat/DEFECT_LOG.md), fix any P1/P2 defects with regression coverage, then obtain pilot lead sign-off in [docs/pilot-assessment.md](/d:/Handover%20Project/docs/pilot-assessment.md) before emitting `PHASE_4_COMPLETE` or starting Phase 5. In parallel, port the design tokens + admin/reports components from `frontend-stubs/` into the Phase 2 Next.js frontend (Admin Users at `app/(admin)/users/page.tsx`, Reports at `app/(app)/reports/page.tsx`).
- Iteration: Phase 4 - Pilot Deployment & UAT

## Completed in This Session
| Date | Task | Notes |
| --- | --- | --- |
| 2026-05-05 | Extended `frontend-stubs/` with Command Palette (Cmd+K) + Shortcuts Overlay ("?") | Added `components/cmdk/{CommandPalette, ShortcutsOverlay}`, `lib/{fuzzy, commands}`, and `hooks/useCommandPalette` (provider + `useRegisterCommands`). Extended `useKeyboardShortcuts` with `onPalette` (Cmd/Ctrl+K, allowed inside text inputs) and made the existing `?` chord toggle the shortcuts overlay. Wired `<AppShell>` to mount `<CommandPaletteProvider>`, register the default-commands set (Dashboard / Log / New Handover / Reports / Admin / Settings / Toggle Theme / Toggle Locale / Show Shortcuts / Sign Out — with `availableWhen` per BR-12 role), and render both overlays at the root. Fuzzy matcher (`lib/fuzzy.ts`) is zero-dependency (~50 lines) with match-at-start/after-separator/consecutive-run scoring. Added `examples/cmdk/page.tsx` demonstrating page-scoped command registration (handover detail commands: Acknowledge / Carry forward / Export CSV). i18n extended with VI/EN keys for command titles, group labels, hints, and overlay strings. README.md gained a full "Command palette + Shortcuts overlay" section. Verified `tsc --noEmit` strict — 0 errors. |
| 2026-05-05 | Extended `frontend-stubs/` with self-service Settings page | Added `components/settings/` (`SettingsLayout` 2-col shell with hash routing `#profile`/`#preferences`/`#security`, `SettingsNav` sidebar, `ProfileSection` with name edit + dirty-state detection + ADMIN-only privileged-fields toggle, `PreferencesSection` for theme/locale/density/default-shift wired to existing `useTheme` + `useI18n` cookies, `SecuritySection` change-password form with current-password gate + server-error-code mapping + optional sign-out-all-sessions card, `PasswordStrengthMeter` pure 4-bar meter using length+char-class heuristic). Extended `lib/types.ts` with `SelfProfile` (no `passwordHash` ever returned), `ProfileUpdateInput`, `ChangePasswordInput`, `Density`, `PreferenceState`. Extended `lib/i18n.ts` with VI/EN settings keys. Added `examples/settings/page.tsx` (drops at `app/(app)/settings/page.tsx`). Updated README.md with full "Settings page (self-service)" section noting that `/api/v1/users/me`, `/api/v1/users/me/password`, and `/api/v1/users/me/sessions/revoke` are NOT yet documented in `shared/API_SPEC.md` — recommended next step for backend before wiring real fetches. Verified `tsc --noEmit` strict — 0 errors. |
| 2026-05-05 | Extended `frontend-stubs/` with NextAuth.js v5 (Auth.js) wiring | Added `auth.config.example.ts` (Edge-safe — providers list left empty so Edge bundle stays free of bcrypt/Prisma; carries `pages.signIn`, JWT/session callbacks that surface `role` + `userId`, and `authorized()` callback enforcing the BR-12 `ROUTE_ROLES` matrix with redirect to `/forbidden` for wrong-role / `/signin` for unauthenticated). Added `auth.example.ts` (full Node-runtime wiring with `@auth/prisma-adapter` + `Credentials` provider; `authorize()` validates with Zod, looks up user by email, rejects when `passwordHash` is null/SSO-only or `isActive=false`, compares with bcrypt). Added `middleware.example.ts` (drop-in middleware that imports `authConfig` and runs on every non-asset path) and `next-auth.d.ts` (module augmentation extending `Session.user` and `JWT` with `id` + `role: UserRole`). Added `lib/auth-helpers.ts` (`getCurrentUser`, `requireUser`, `requireRole`, `requireCapability` — all server-side, throw a `NEXT_REDIRECT` digest when failing). Added `components/auth/` (`SignInLayout` branded shell using `useShiftTheme`, `SignInForm` controlled credentials form with Auth.js error-code → i18n mapping, `UserMenu` avatar dropdown for TopBar with click-outside + Escape close, `RoleSwitcher` dev-only impersonation gated to `NODE_ENV !== 'production'`, `UnauthorizedView` 403 page). Added 3 example route stubs under `examples/auth/` (signin/page.tsx, forbidden/page.tsx, route.example.ts → `app/api/auth/[...nextauth]/route.ts`). Extended `lib/i18n.ts` with VI/EN auth strings. Updated `README.md` with full "NextAuth.js v5 (Auth.js) wiring" section explaining the Edge/Node split, env vars, module augmentation, server-side guard usage, and how to swap Prisma for an Express `/api/v1/auth/login` backend. Verified `tsc --noEmit` strict — 0 errors with `next-auth@5.0.0-beta.20`, `@auth/prisma-adapter@2.4.2`, `bcryptjs@2.4.3`, `zod@3.23.8`, `@prisma/client@5.18.0` in the sandbox. |
| 2026-05-05 | Extended `frontend-stubs/` with Admin Users + Reports/Export | Added `components/admin/` (UsersTable, UserFilters, UserFormDialog, UserDeactivateDialog) backed by `GET\|POST /api/v1/users` + `PATCH/DELETE /api/v1/users/:id` (soft-delete via `isActive=false`, never hard-delete per AGENTS.md). Added `components/reports/` (ReportFilters, ReportPreview with print-region wrapper, ExportButton with PDF/CSV split + browser print pipe) backed by `GET /api/v1/handovers/export/csv`. Extended `lib/types.ts` with `UserDetail` (no `passwordHash` ever leaves server), `UserCreateInput`, `UserUpdateInput`, `UserFiltersValue`, `ExportFormat`, `ReportFiltersValue`, `ReportDataset`. Added `@media print` rules to `styles.global.example.css` (white-on-black, hide chrome via `print:hidden`, page breaks). Added `overlay` token to design system. Two new example pages: `examples/admin/page.tsx` (uses `can(role, 'manageUsers')` guard) + `examples/reports/page.tsx`. Type-checked clean with `tsc --noEmit` strict. |
| 2026-05-05 | Added React + Tailwind component stubs under `frontend-stubs/` | TypeScript-strict React 18 + Next.js 14 (App Router) port of the `prototype/` UX. Includes design tokens (`tailwind.config.example.ts` + `styles.global.example.css`), `lib/types.ts` matching `shared/DATA_MODEL.md` 1:1, BR-12 capability matrix (`lib/permissions.ts`), VI/EN i18n, `useTheme` (cookie-backed, no `localStorage` per AGENTS.md "Do Not"), `useShiftTheme`, `useKeyboardShortcuts`, `<AppShell>`, `<TopBar>`, `<Sidebar>`, `<CriticalBanner>`, `<DashboardKpis>`, `<HandoverTable>`, `<HandoverHeader>`, `<HandoverFilters>`, `<CategorySection>` (with `toItemView()` discriminated-union helper), `<AcknowledgeButton>` (BR-10 self-ack guard), `<AuditTrail>`, `<CarryForwardLink>` (BR-07), 3-step `<HandoverWizard>` (BR-02 — never sets `referenceId`), and `<StatusTimeline>` (BR-05). Four example pages under `examples/` map directly to App Router routes. Type-checked clean with `tsc --noEmit` against Next.js 14.2.5 + React 18.3.1 + TypeScript 5.4.5 (strict). Designed to be copy-pasted into the Phase 2 Next.js frontend. Non-blocking — does not gate phase progression. |
| 2026-05-05 | Added Friendly Redesign UI prototype under `prototype/` | Browser-only HTML/CSS/JS (no build step). Includes Login (role grid), Dashboard (KPI cards + 4 charts + smart filter chips), Handover Log (filter row + table), Handover Detail (header + items with status timeline + audit timeline + ack action), New Handover (3-step stepper), Audit Trail, Help. Light + dark theme, shift-aware accent (Morning/Afternoon/Night), critical-not-yet-ack banner, carry-forward visual, mobile responsive with FAB, keyboard shortcuts (N/L/D/A/T/?), VI/EN toggle, print-ready CSS, mock 5 UAT scenarios + 1 normal shift seeded into localStorage. Verified in Chrome at `http://localhost:8765/`. Non-blocking design proposal — does not gate phase progression. |
| 2026-04-24 | Drafted Task 4.7 Go/No-Go evidence | Added [docs/pilot-assessment.md](/d:/Handover%20Project/docs/pilot-assessment.md) with Task 4.1 no-Docker staging evidence, UAT verifier evidence, smoke results, performance results, defect summary, criteria table, and a GO recommendation for the no-Docker Phase 4 pilot path. It still records human UAT timing/feedback and pilot lead sign-off as pending before `PHASE_4_COMPLETE`. |
| 2026-04-24 | Removed Docker as a Task 4.1 acceptance gate | Updated [phases/PHASE_4.md](/d:/Handover%20Project/phases/PHASE_4.md), [docs/uat/UAT_SCENARIOS.md](/d:/Handover%20Project/docs/uat/UAT_SCENARIOS.md), and [infrastructure/staging/README.md](/d:/Handover%20Project/infrastructure/staging/README.md) so Task 4.1 is accepted through the no-Docker local production-mode staging path. Docker/compose files remain optional scaffolding only. |
| 2026-04-24 | Fixed Task 4.6 BUG-001 | After reseeding UAT, `npm run test:smoke` found `POST /api/v1/handovers` returning `500` from a Prisma `P2002` duplicate `referenceId`. Updated [database/prisma/uat-seed.ts](/d:/Handover%20Project/database/prisma/uat-seed.ts) so UAT refs synchronize and allocate through `handover_reference_seq`, then reran `npm run db:seed:uat` and `npm run test:smoke` successfully outside sandbox. |
| 2026-04-24 | Added UAT seed verifier for Task 4.6 regression coverage | Added [scripts/verify-uat-scenarios.mjs](/d:/Handover%20Project/scripts/verify-uat-scenarios.mjs) plus root `npm run verify:uat`. The verifier checks UAT users, 5 active scenarios, scenario-specific item/audit shape, today Afternoon availability for Scenario 1, and `handover_reference_seq` health. Outside sandbox, `npm run verify:uat` passed with max reference suffix `1377` and next sequence value `1378`. |
| 2026-04-24 | Refreshed UAT references for Task 4.6 | Latest active UAT refs are now `HDO-2026-001371` through `HDO-2026-001375`, and [docs/uat/UAT_SCENARIOS.md](/d:/Handover%20Project/docs/uat/UAT_SCENARIOS.md) has been updated to match. |
| 2026-04-24 | Started Task 4.6 defect tracking | Added [docs/uat/DEFECT_LOG.md](/d:/Handover%20Project/docs/uat/DEFECT_LOG.md) with severity definitions, status definitions, defect table, and pilot sign-off snapshot. The initial tracker had no reported defects before BUG-001 was discovered during reseed/smoke verification. |
| 2026-04-24 | Captured Task 4.6 baseline evidence | Outside sandbox, `npm run verify:staging:local` passed and `npm run test:smoke` passed against the live local app. Sandbox attempts hit environment-only Prisma process/TLS restrictions, so live Supabase-backed verification still needs outside-sandbox execution. |
| 2026-04-24 | Completed Task 4.5 performance optimization | Consolidated [backend/src/services/dashboard.service.ts](/d:/Handover%20Project/backend/src/services/dashboard.service.ts) into a single aggregate query, added a primitive-cast raw SQL fast path in [backend/src/services/handover.service.ts](/d:/Handover%20Project/backend/src/services/handover.service.ts), simplified create reference generation/pre-create checks, raised `DATABASE_CONNECTION_LIMIT` to `20`, and reran `npm run perf:check` successfully. |
| 2026-04-24 | Captured final Task 4.5 benchmark evidence | Final `npm run perf:check` outside sandbox passed all scoped endpoints: dashboard `166.50ms` avg / `202.88ms` P95 / `244.82ms` P99, handover list `170.45ms` avg / `217.67ms` P95 / `284.85ms` P99, create `423.43ms` avg / `470.21ms` P95 / `669.42ms` P99, all with `0` errors. |
| 2026-04-24 | Removed Prisma pool starvation from the local pilot path | Added [scripts/apply-database-runtime-config.mjs](/d:/Handover%20Project/scripts/apply-database-runtime-config.mjs), routed root scripts through [scripts/run-node-with-root-env.mjs](/d:/Handover%20Project/scripts/run-node-with-root-env.mjs), set `DATABASE_CONNECTION_LIMIT=10`, and verified outside sandbox with `npm run verify:staging:local`, `npm run db:seed:uat`, and `npm run test:smoke`. |
| 2026-04-24 | Fixed dashboard summary `500` errors during Task 4.5 | Updated [backend/src/services/dashboard.service.ts](/d:/Handover%20Project/backend/src/services/dashboard.service.ts) to cast raw SQL enum parameters explicitly, rebuilt the backend, restarted the local staging app, and confirmed `dashboard/summary` stopped failing with PostgreSQL `42883`. |
| 2026-04-24 | Ran the second Task 4.5 benchmark pass | Reran `npm run perf:check` outside sandbox against `http://localhost:3000` and `http://localhost:4000`. All three endpoints still miss latency targets, but error counts dropped to zero and [docs/performance-report.md](/d:/Handover%20Project/docs/performance-report.md) now contains first-pass versus second-pass comparison notes. |
| 2026-04-24 | Ran the first Task 4.5 benchmark pass | Added [scripts/perf-check.mjs](/d:/Handover%20Project/scripts/perf-check.mjs) plus root `npm run perf:check`, then ran the first no-Docker benchmark pass outside sandbox against `http://localhost:3000` and `http://localhost:4000`. The run failed all three targets and exposed a Prisma connection pool bottleneck (`P2024`, `connection_limit: 1`) plus one `P2028` concurrent write failure. |
| 2026-04-24 | Hardened the UAT seed for slower remote transactions | Updated [database/prisma/uat-seed.ts](/d:/Handover%20Project/database/prisma/uat-seed.ts) to increase Prisma interactive transaction `maxWait` and `timeout`, then reran `npm run db:seed:uat` successfully after the earlier `5000ms` timeout failure. |
| 2026-04-24 | Documented the no-Docker Task 4.5 plan | Added [docs/performance-report.md](/d:/Handover%20Project/docs/performance-report.md) with the fallback local execution path, endpoint targets, auth strategy, staged concurrency plan, cleanup rules, and a results table ready for the first benchmark pass. |
| 2026-04-24 | Implemented and verified Task 4.4 automated smoke tests | Added [tests/smoke/smoke.test.ts](/d:/Handover%20Project/tests/smoke/smoke.test.ts) plus root `npm run test:smoke`. Outside sandbox, the smoke flow passed against the live local app at `http://localhost:3000` and `http://localhost:4000`, covering frontend login reachability, backend health, create handover, automatic carry-forward, acknowledgment, and soft-archive cleanup of the test-created handovers. |
| 2026-04-24 | Prepared the no-Docker local UAT path end-to-end | Outside sandbox, `npm run build`, `npm run verify:staging:local`, and `npm run db:seed:uat` all passed. After reseed, local production app was started with `npm start`, and both `http://localhost:3000/login` and `http://localhost:4000/health` responded with `200`. |
| 2026-04-24 | Implemented Task 4.3 UAT test scripts | Added [docs/uat/UAT_SCENARIOS.md](/d:/Handover%20Project/docs/uat/UAT_SCENARIOS.md) with 5 human-readable scripts, exact `@occ.test` credentials, current seed reference guidance, and no-Docker execution notes for local or shared staging use. |
| 2026-04-24 | Implemented Task 4.2 UAT scenario data loader | Added [database/prisma/uat-seed.ts](/d:/Handover%20Project/database/prisma/uat-seed.ts) plus root `npm run db:seed:uat`. The loader seeds dedicated `staff@occ.test`, `supervisor@occ.test`, `viewer@occ.test`, and `admin@occ.test` users with password `Pilot2025!`, archives prior active UAT scenarios with soft-delete audit logs, and creates 5 tagged UAT handovers. |
| 2026-04-24 | Verified Task 4.2 against Supabase | Outside sandbox, `tsx database/prisma/uat-seed.ts` succeeded twice. The second run archived 5 previous active UAT handovers and reseeded a clean active set, confirming the loader is safe to rerun. |
| 2026-04-24 | Confirmed the live UAT dataset shape | Read-only Supabase verification confirmed 5 active UAT handovers, Scenario 2 is `High` + `isCarriedForward = true` with 2 `CARRIED_FORWARD` audit logs, Scenario 4 is `Resolved`, and the latest active refs are `HDO-2026-000016` through `HDO-2026-000020`. |
| 2026-04-23 | Implemented Task 4.1 staging scaffolding | Added root `Dockerfile`, `.dockerignore`, `scripts/start-staging.mjs`, root `npm start` plus `db:migrate:deploy`, `infrastructure/staging/docker-compose.staging.yml`, staging env example/README, and `.github/workflows/deploy-staging.yml`. The staging app container now runs Prisma deploy migrations automatically, starts backend on `:4000`, and serves the frontend on `:3000`. |
| 2026-04-23 | Verified Task 4.1 at code/config level | `npm test` passes, outside-sandbox `npm run build` passes, `docker compose ... config` passes with the staging env example, and `scripts/start-staging.mjs` passes Node syntax validation. |
| 2026-04-23 | Attempted live Docker image verification | `docker build` could not be completed in the current session because the local Docker Desktop Linux engine pipe (`dockerDesktopLinuxEngine`) was unavailable. |
| 2026-04-23 | Diagnosed the Task 4.1 host blocker | Docker Desktop is installed and its processes can start, but `wsl --status` reports that WSL is not installed on this Windows host. Because Docker Desktop is configured for the `desktop-linux` context, the Linux engine cannot come up and live staging verification remains blocked outside the repo. |
| 2026-04-23 | Added no-admin fallback for Task 4.1 verification | Added `scripts/verify-local-staging.mjs` plus `npm run verify:staging:local` for a local production-mode sanity check without Docker, and upgraded the staging workflow to boot the full compose stack on `ubuntu-latest` with container health checks and automatic teardown. |
| 2026-04-23 | Fixed staging startup/runtime issues found by the no-admin verifier | Updated `scripts/start-staging.mjs` so `next start` runs from `frontend/` and checks for `frontend/.next/BUILD_ID`; switched readiness probes to public endpoints; and enabled `trustHost` in NextAuth so self-hosted staging no longer emits `UntrustedHost` errors. |
| 2026-04-23 | Verified the no-admin local staging path end-to-end | Outside sandbox, `npm run build` followed by `npm run verify:staging:local` now starts migrations, backend, and frontend successfully and confirms readiness within 60 seconds on this Windows host without Docker. |
| 2026-04-23 | Completed Phase 3 manual gate verification | Ran end-to-end verification against seeded Supabase data through the local backend using signed auth headers. Auto carry-forward created `HDO-2026-000009` on `2026-04-19` Afternoon from source `HDO-2026-000001` with 3 carried items; acknowledgment verification created `HDO-2026-000010` on `2026-04-23` Night and recorded acknowledgments from both `OCC Staff` and `Shift Supervisor` while preserving the first `acknowledgedAt` timestamp. |
| 2026-04-23 | Declared `PHASE_3_COMPLETE` | Phase 3 gate checks are now satisfied: `npm test` passes, `npm run build` passes, carry-forward was tested end-to-end, and acknowledgment was tested with two different user sessions. |

## Work In Progress
| Task | Progress | Main Files | Notes |
| --- | --- | --- | --- |
| Task 4.1 - Staging Environment Setup | 100% | [scripts/start-staging.mjs](/d:/Handover%20Project/scripts/start-staging.mjs), [scripts/verify-local-staging.mjs](/d:/Handover%20Project/scripts/verify-local-staging.mjs), [frontend/lib/auth.config.ts](/d:/Handover%20Project/frontend/lib/auth.config.ts), `package.json`, optional `infrastructure/staging/` and `.github/workflows/deploy-staging.yml` | Complete for Phase 4. Docker is no longer an acceptance gate; the accepted staging evidence is the passing no-Docker production-mode local path plus reachable frontend/backend URLs. |
| Task 4.5 - Performance & Load Check | 100% | [docs/performance-report.md](/d:/Handover%20Project/docs/performance-report.md), [scripts/perf-check.mjs](/d:/Handover%20Project/scripts/perf-check.mjs), [scripts/apply-database-runtime-config.mjs](/d:/Handover%20Project/scripts/apply-database-runtime-config.mjs), [backend/src/services/dashboard.service.ts](/d:/Handover%20Project/backend/src/services/dashboard.service.ts), [backend/src/services/handover.service.ts](/d:/Handover%20Project/backend/src/services/handover.service.ts) | Complete for the no-Docker pilot path. Final benchmark passed all scoped endpoints with zero errors and all averages/P95/P99 below target. |
| Task 4.6 - Defect Tracking & Fix | 70% | [docs/uat/DEFECT_LOG.md](/d:/Handover%20Project/docs/uat/DEFECT_LOG.md), [database/prisma/uat-seed.ts](/d:/Handover%20Project/database/prisma/uat-seed.ts), [scripts/verify-uat-scenarios.mjs](/d:/Handover%20Project/scripts/verify-uat-scenarios.mjs), [docs/uat/UAT_SCENARIOS.md](/d:/Handover%20Project/docs/uat/UAT_SCENARIOS.md), [tests/smoke/smoke.test.ts](/d:/Handover%20Project/tests/smoke/smoke.test.ts) | Defect log exists. BUG-001 P1 was fixed with regression evidence (`npm run db:seed:uat` + `npm run test:smoke` + `npm run verify:uat`). Pending human browser UAT execution and any newly reported P1/P2 fixes. |
| Task 4.7 - Go/No-Go Assessment | 80% | [docs/pilot-assessment.md](/d:/Handover%20Project/docs/pilot-assessment.md) | Evidence report exists and recommends GO for the no-Docker Phase 4 pilot path based on automated verification. Pending human UAT feedback/timing and pilot lead sign-off before Phase 4 can be declared complete. |

## Blockers
| Issue | Cause | Plan | Owner |
| --- | --- | --- | --- |
| Pilot lead sign-off not yet recorded | Task 4.7 includes a manual human sign-off step after UAT findings are reviewed | Record human UAT outcome and pilot lead approval in `docs/pilot-assessment.md`; then emit `PHASE_4_COMPLETE` if no blocking defects remain | Pilot lead |

## Recent Decisions
| Date | Decision | Why |
| --- | --- | --- |
| 2026-04-24 | Remove Docker as a Task 4.1 acceptance gate | The user explicitly scoped Task 4.1 away from Docker. The no-Docker local production-mode path has already passed build/startup/readiness checks and is the path used for smoke, UAT verifier, and performance evidence. Docker/compose artifacts can remain optional scaffolding, but they no longer block Phase 4 acceptance. |
| 2026-04-24 | Set `DATABASE_CONNECTION_LIMIT=20` for the long-lived local/staging app path | The acceptance read profile uses `20` concurrent clients. With query fan-out removed, pool `10` still queued requests; pool `20` matched the benchmark profile and kept all endpoints below target without errors. |
| 2026-04-24 | Allocate UAT handover references through the production sequence | Task 4.6 BUG-001 showed that max-suffix reference allocation in the UAT loader could fall out of sync with `handover_reference_seq`, so the loader now synchronizes and allocates through the same monotonic sequence used by runtime creation. |
| 2026-04-24 | Return primitive-cast values from the handover list fast path | Read-only timing showed PostgreSQL executed the list SQL in under `1ms`, while Prisma native Date/enum decoding kept the endpoint around `300ms+`. Casting dates/enums to text/epoch in SQL preserved the API shape and reduced list latency. |
| 2026-04-24 | Keep Task 4.5 benchmark as no-Docker pilot evidence | This host still cannot run Linux Docker compose, but the verified local production-mode path exercises the real Supabase-backed backend/database behavior needed for pilot UAT. |
| 2026-04-24 | Introduce `DATABASE_CONNECTION_LIMIT` as a runtime override for long-lived local/staging app processes | The shared `.env` was carrying a serverless-style `connection_limit=1` on the Supabase pooler URL, which starved Prisma under pilot load. A runtime override keeps the pooled URL reusable while giving the local no-Docker app enough headroom for benchmark and UAT traffic. |
| 2026-04-24 | Cast dashboard raw SQL enum parameters explicitly | PostgreSQL rejected text-vs-enum comparisons inside the dashboard raw queries with `42883`, so the dashboard summary route needed explicit enum casts before the second benchmark pass could be trusted. |
| 2026-04-24 | Implement Task 4.5 as a standalone `scripts/perf-check.mjs` runner instead of a Vitest performance suite | A standalone script is easier to run repeatedly against the live local URLs, easier to give staged load profiles, and easier to reuse for manual pilot reruns |
| 2026-04-24 | Plan Task 4.5 around the no-Docker local path and direct backend `:4000` endpoints | This Windows host can run the full app without Docker, and direct backend load tests avoid frontend proxy/session gaps while still measuring the real database-backed pilot path |
| 2026-04-24 | Write Task 4.3 around the real current UI and the Handover Log search path | Recent dashboard cards are not clickable today, so the UAT scripts need a stable route that testers can actually follow |
| 2026-04-24 | Document no-Docker UAT execution directly in `docs/uat/UAT_SCENARIOS.md` | The current Windows host cannot run Docker compose, but human UAT can still proceed through the verified local production-mode path or a shared staging URL |
| 2026-04-24 | Seed UAT with dedicated `@occ.test` accounts and one shared `Pilot2025!` password | Lets Task 4.3 reference exact reusable credentials without mixing UAT logins with dev/demo accounts |
| 2026-04-24 | Soft-archive prior active UAT scenarios before reseeding | Keeps `database/prisma/uat-seed.ts` rerunnable and avoids hard deletes while preserving audit history |
| 2026-04-24 | Place the seeded crew scenario on yesterday Afternoon instead of today Afternoon | Keeps today Afternoon open so Scenario 1 can verify live carry-forward by creating that handover through the UI |
| 2026-04-23 | Model staging as one `app` container plus one PostgreSQL `db` container | Matches the Phase 4 shape while fitting the current repo, where frontend and backend remain separate Node processes |
| 2026-04-23 | Run Prisma deploy migrations inside `npm start` before launching frontend/backend | Satisfies the Task 4.1 requirement that database migrations run automatically on startup |
| 2026-04-23 | Keep frontend and backend in the same staging container, with frontend on `:3000` and backend on `:4000` internally | Keeps staging deployment simpler while preserving the production-like request path used by the current Next.js server/API proxies |
| 2026-04-23 | Validate staging compose in CI with `.env.staging.example` before image build | Catches env/compose regression early even when a full deploy host is not involved |

## Next Steps
1. Run browser UAT against the live local app at `http://localhost:3000`, using [docs/uat/UAT_SCENARIOS.md](/d:/Handover%20Project/docs/uat/UAT_SCENARIOS.md).
2. Record every human UAT finding in [docs/uat/DEFECT_LOG.md](/d:/Handover%20Project/docs/uat/DEFECT_LOG.md), then fix any P1/P2 defects with regression tests before pilot sign-off.
3. Use `npm run db:seed:uat`, `npm run verify:uat`, and `npm run test:smoke` whenever the local pilot dataset needs a clean refresh plus automated sanity coverage.
4. Rerun `npm run perf:check` only if Task 4.6 changes touch dashboard, handover list, create handover, auth, reference ID generation, or Prisma runtime behavior.
5. Update [docs/pilot-assessment.md](/d:/Handover%20Project/docs/pilot-assessment.md) with human UAT timing/feedback and pilot lead sign-off; emit `PHASE_4_COMPLETE` only after the pending manual criteria are satisfied.

## Notes for the Next Session
- Phase 1 is complete and frozen.
- Phase 2 is complete.
- Phase 3 is complete. `PHASE_3_COMPLETE`.
- Phase 4 has started. Task 4.1 is complete for the no-Docker pilot path, and Tasks 4.2, 4.3, 4.4, and 4.5 are complete.
- `shared/DATA_MODEL.md`, `shared/BUSINESS_RULES.md`, `shared/API_SPEC.md`, `shared/roles.md`, `shared/schemas-design.md`, and `shared/setup-guide.md` remain the implementation baseline.
- Database is hosted on Supabase for current dev/pilot work; Docker compose remains optional scaffolding and is no longer a Task 4.1 acceptance gate.
- `npm test` passes with 50 root tests and 43 frontend tests.
- Outside-sandbox `npm run build` passes for both frontend and backend.
- Outside-sandbox `npm run verify:staging:local` now passes on this Windows host without Docker or WSL.
- Outside-sandbox `npm run test:smoke` now passes on the live local app, covering frontend login reachability, backend health, create handover, automatic carry-forward, acknowledgment, and soft-archive cleanup.
- Task 4.5 final evidence now lives in [docs/performance-report.md](/d:/Handover%20Project/docs/performance-report.md), with the runner in [scripts/perf-check.mjs](/d:/Handover%20Project/scripts/perf-check.mjs).
- Task 4.6 tracking now lives in [docs/uat/DEFECT_LOG.md](/d:/Handover%20Project/docs/uat/DEFECT_LOG.md). It currently records BUG-001 as fixed, zero open P1 defects, and zero open P2 defects, but this still needs human UAT confirmation.
- Task 4.7 evidence now lives in [docs/pilot-assessment.md](/d:/Handover%20Project/docs/pilot-assessment.md). It recommends GO for the no-Docker Phase 4 pilot path based on automated evidence, with human UAT feedback/timing and pilot lead sign-off still pending.
- Task 4.6 now has `npm run verify:uat` for UAT dataset and reference-sequence verification. Run it after `npm run db:seed:uat` and before human UAT when the dataset is refreshed.
- `DATABASE_CONNECTION_LIMIT=20` is now applied through [scripts/run-node-with-root-env.mjs](/d:/Handover%20Project/scripts/run-node-with-root-env.mjs) and [scripts/start-staging.mjs](/d:/Handover%20Project/scripts/start-staging.mjs) for the long-lived local/staging process path.
- The final benchmark pass met every Task 4.5 target with zero errors: dashboard `166.50ms` avg, list `170.45ms` avg, and create `423.43ms` avg at the acceptance load profiles.
- Local production app is currently reachable for browser UAT at `http://localhost:3000`; backend health check is reachable at `http://localhost:4000/health`.
- Outside-sandbox `npm run verify:staging:local` and `npm run test:smoke` both passed again after creating the Task 4.6 defect log.
- Task 4.3 now lives in [docs/uat/UAT_SCENARIOS.md](/d:/Handover%20Project/docs/uat/UAT_SCENARIOS.md).
- The UAT doc includes exact credentials, 5 scenario scripts, current verified refs, and a no-Docker execution section.
- Task 4.4 now lives in [tests/smoke/smoke.test.ts](/d:/Handover%20Project/tests/smoke/smoke.test.ts), with root script `npm run test:smoke`.
- The smoke test uses the live local URLs plus signed backend headers for the seeded `@occ.test` users; it creates two temporary handovers, verifies carry-forward and acknowledgment, then soft-archives the test-created records.
- Scenario 1 and Scenario 3 scripts were adjusted to match the current app behavior: use `Handover Log` search instead of clicking recent dashboard cards, and validate owner enforcement through a fresh handover creation attempt.
- Root package now includes `npm run db:seed:uat`, backed by [database/prisma/uat-seed.ts](/d:/Handover%20Project/database/prisma/uat-seed.ts).
- The UAT loader was verified twice against Supabase; the second run archived 5 previous active UAT handovers and reseeded 5 active scenarios.
- The UAT loader now has longer interactive transaction timeouts so reseeds remain stable even when Supabase latency spikes briefly.
- Latest verified active UAT refs are `HDO-2026-001371` to `HDO-2026-001375`.
- UAT credentials are: `staff@occ.test`, `supervisor@occ.test`, `viewer@occ.test`, and `admin@occ.test`, all using password `Pilot2025!`.
- Scenario 2 (`HDO-2026-001372`) is the carried-forward acknowledgment case: `High` priority, `isCarriedForward = true`, and 2 `CARRIED_FORWARD` audit log entries.
- Today Afternoon is intentionally left free in the seeded dataset so Scenario 1 can create it live and verify automatic carry-forward from the Morning AOG handover.
- Docker/compose artifacts remain in the repo as optional deployment scaffolding, but Docker is no longer required for Task 4.1 or Phase 4 Go/No-Go evidence.
- Use `npm run verify:staging:local` for the accepted no-admin local staging sanity check.
- Dev verification records still exist in Supabase: `HDO-2026-000009` and `HDO-2026-000010`.
